# Dotfiles

## Recommended installation

- Clone these files to ~/dotfiles
- Install `stow` with `brew install stow` (on macOS)
- Inside ~/dotfiles, run `stow .`

This will symlink all of the files in ~/dotfiles to ~.

### Install the following:

`
brew install eza fzf bat
`
